// actions are exported from this file

import {
    fetchPosts,
    fetchedPosts,
    failedFetchPosts,

    fetchCreatePosts,
    fetchedCreatePosts,
    failedFetchCreatePosts,
} from "./posts";

export {
    fetchPosts,
    fetchedPosts,
    failedFetchPosts,

    fetchCreatePosts,
    fetchedCreatePosts,
    failedFetchCreatePosts,
}